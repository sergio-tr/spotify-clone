export function Arrow({text}) {

    return (
        <button className="w-12 h-12 rounded-full bg-zinc-500/60 hover:bg-zinc-800 p-2 hover:scale-105 transition text-2xl">
            {text} 
        </button>
    )
}